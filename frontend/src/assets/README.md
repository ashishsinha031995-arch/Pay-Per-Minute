# Frontend Assets

This directory contains compiled assets imported directly into React:
- `/images` - local illustration images
- `/icons` - custom icon SVGs
- `/animations` - dynamic web animations (such as Lottie files)
